norn_ett:
  target: local
  outputs:
    local:
      type: clickhouse
      driver: http
      host: "{{ env_var('CH_HOST', 'localhost') }}"
      port: 8123
      user: norn
      password: norn
      schema: norn_ett
      secure: false
